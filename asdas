from pathlib import Path
import glob
import tifffile
from tifffile import imread, imwrite
import os
import numpy as np

dirpath = r"D:\CG_VGATM1_HEATSTEPS_28032022-000"
out_fp = r"D:\CG_VGATM1_HEATSTEPS_28032022-000"

search_criteria = "*.tif"
q = os.path.join(dirpath, search_criteria)

tiff_lst = glob.glob(q)

for tiff in tiff_lst:
	sc = tifffile.imread(tiff)
	sc_files.appened(sc)
	tifffile.imsave("out.tif")
